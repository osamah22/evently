info:
  name: delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/categories/{{categoryId}}"
  auth: inherit

runtime:
  variables:
    - name: categoryId
      value: "19"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
